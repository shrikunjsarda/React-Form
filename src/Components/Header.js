import React from 'react'
import StyledHeader from './StyledHeader'

function Header() {
  // Use it like any other component.
  return <StyledHeader> Survey Form </StyledHeader>;
}

export default Header